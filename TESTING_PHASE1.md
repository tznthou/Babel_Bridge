# Phase 1 測試計劃

## 🎯 測試目標
驗證完整音訊處理管線的功能正確性：
- ✅ 音訊擷取 (chrome.tabCapture)
- ✅ Rolling Window 切塊 (3秒音訊 + 1秒重疊)
- ✅ MP3 編碼 (Web Worker)
- ✅ Whisper API 辨識
- ✅ OverlapProcessor 去重與斷句
- ✅ Content Script 時間同步字幕顯示

---

## 📋 測試步驟

### **階段 1: 前置準備** (預計 5 分鐘)

#### 1.1 確認建置產物
```bash
# 檢查 dist/ 資料夾內容
ls -la dist/

# 應該包含以下檔案：
# - manifest.json
# - src/background/service-worker.js (及所有相關模組)
# - src/content/content-script.js
# - src/content/subtitle-overlay.css
# - src/popup/popup.html, popup.js, popup.css
# - src/workers/mp3-encoder.worker.js
# - src/lib/ (所有核心函式庫)
```

#### 1.2 載入 Extension
1. 開啟 Chrome: `chrome://extensions/`
2. 開啟「開發人員模式」(右上角)
3. 如果已載入舊版本，點擊「重新載入」(Reload)
4. 如果是首次載入，點擊「載入未封裝項目」→ 選擇 `dist/` 資料夾

**驗證點**:
- ✅ Extension 圖示出現在工具列
- ✅ Console 無載入錯誤
- ✅ 檢查 `chrome://extensions/` 確認版本正確

#### 1.3 設定 API Key
1. 點擊 Extension 圖示開啟 Popup
2. 輸入你的 OpenAI API Key (格式: `sk-proj-...`)
3. 點擊「驗證」

**驗證點**:
- ✅ 驗證成功訊息出現
- ✅ API Key 以遮罩形式顯示 (`sk-proj-****...****`)
- ✅ Console 顯示 `[APIKeyManager] API Key 驗證成功`

---

### **階段 2: 基礎功能測試** (預計 5 分鐘)

#### 2.1 準備測試影片
開啟一個包含影片的網站，建議使用：
- YouTube 短片 (2-3 分鐘，有清晰語音)
- 或使用 Demo 頁面: `demo/overlap-processor-demo.html`

**推薦測試影片**:
- 簡單英文: TED Talk 片段
- 中文: 新聞片段
- 避免使用背景音樂過大或多人同時說話的影片

#### 2.2 啟用字幕功能
1. 在影片頁面點擊 Extension 圖示
2. 點擊「啟用字幕」按鈕
3. 允許「擷取音訊」權限 (Chrome 會彈出權限請求)

**驗證點**:
- ✅ Popup 顯示「字幕已啟用」
- ✅ 影片繼續正常播放
- ✅ Console 顯示初始化訊息

---

### **階段 3: 音訊處理管線測試** (預計 10 分鐘)

#### 3.1 開啟 DevTools Console
1. 在影片頁面按 `F12` 或 `Cmd+Option+I`
2. 切換到 Console tab
3. 開啟 Service Worker Console (用於查看 Background 日誌):
   - 前往 `chrome://extensions/`
   - 找到 Babel Bridge → 點擊「Service Worker」連結

**你會看到兩個 Console**:
- **Content Script Console** (影片頁面): 顯示字幕顯示相關日誌
- **Service Worker Console**: 顯示音訊處理管線日誌

#### 3.2 觀察音訊處理流程
播放影片並觀察 Service Worker Console，應該按順序出現以下日誌：

```
[SubtitleService] Service Worker 已啟動
[SubtitleService] 服務初始化完成
[MP3Encoder] Worker 已就緒
[WhisperClient] Whisper Client 已初始化

// 每 2 秒一個 chunk (3秒音訊，重疊1秒)
[SubtitleService] 處理 Chunk 0 { startTime: 0.00, endTime: 3.00 }
[SubtitleService] MP3 編碼完成 { size: XXXXX }
[SubtitleService] Whisper 辨識完成 { text: "...", segments: X }
[SubtitleService] OverlapProcessor 處理完成 { originalSegments: X, processedSegments: Y, filtered: Z }

[SubtitleService] 處理 Chunk 1 { startTime: 2.00, endTime: 5.00 }
...
```

**驗證點**:
- ✅ Chunk 間隔約 2 秒 (3秒音訊 - 1秒重疊)
- ✅ MP3 編碼時間 < 500ms
- ✅ Whisper API 響應時間 2-3 秒
- ✅ OverlapProcessor 過濾率 15-25% (filtered / originalSegments)

#### 3.3 觀察字幕顯示
切換到 Content Script Console (影片頁面)，應該看到：

```
[ContentScript] Content script 已載入
[ContentScript] Subtitle overlay 已初始化
[VideoMonitor] 已附加到 video 元素

// 接收字幕資料
[ContentScript] 接收字幕資料: { chunkIndex: 0, segments: X, startTime: 0.00, endTime: 3.00 }
[ContentScript] 目前總共有 X 個 segments

// 根據影片時間顯示字幕
[ContentScript] 顯示字幕: "Hello world" (0.50s - 2.30s)
[ContentScript] 顯示字幕: "This is a test" (2.30s - 4.10s)
...
```

**驗證點**:
- ✅ VideoMonitor 成功附加到 video 元素
- ✅ segments 持續累積 (總數持續增加)
- ✅ 字幕根據 video.currentTime 動態顯示
- ✅ 字幕文字正確出現在影片下方

---

### **階段 4: 字幕時間同步測試** (預計 5 分鐘)

#### 4.1 播放/暫停測試
1. **播放影片** → 觀察字幕是否隨時間變化
2. **暫停影片** → 字幕應停留在當前句子
3. **繼續播放** → 字幕應從正確位置繼續

**驗證點**:
- ✅ Console 顯示 `[VideoMonitor] 影片開始播放`
- ✅ Console 顯示 `[VideoMonitor] 影片暫停`
- ✅ 字幕與影片狀態同步

#### 4.2 跳轉測試
1. **拖動進度條** 跳到影片中間 (例如 30 秒處)
2. 觀察字幕是否立即更新到對應時間點

**驗證點**:
- ✅ Console 顯示 `[VideoMonitor] 影片跳轉到 30.00`
- ✅ 字幕立即顯示 30 秒處的句子 (不會有舊句子殘留)

#### 4.3 視覺驗證
1. 確認字幕容器位置正確 (影片下方居中)
2. 確認字幕樣式正確:
   - 黑色半透明背景 (`rgba(0, 0, 0, 0.85)`)
   - 白色文字
   - 圓角邊框
   - 淡入動畫

**在 DevTools Elements tab 檢查**:
```html
<div id="babel-bridge-subtitle-overlay" class="babel-subtitle-container" style="display: flex;">
  <div class="babel-subtitle">Hello world</div>
</div>
```

---

### **階段 5: OverlapProcessor 驗證** (預計 5 分鐘)

#### 5.1 檢查過濾率
在 Service Worker Console 搜尋 `OverlapProcessor 處理完成`，統計多個 chunk 的過濾率：

**預期結果**:
```
Chunk 0: filtered: 0 (第一個 chunk 無重疊區)
Chunk 1: filtered: 2-3 (約 15-25%)
Chunk 2: filtered: 2-4 (約 15-25%)
...
```

**異常指標**:
- ❌ 過濾率 > 40%: `similarityThreshold` 設定過低
- ❌ 過濾率 < 5%: Whisper 在重疊區產生完全不同結果 (音訊品質問題)

#### 5.2 檢查斷句品質
觀察 Content Script Console 顯示的字幕文字：

**良好斷句範例**:
```
✅ "Hello, how are you doing today?"
✅ "This is a complete sentence."
```

**不良斷句範例** (如果出現表示有問題):
```
❌ "Hello, how are you do"  // 句子被截斷
❌ "ing today? This is a"   // 跨句子切割
```

---

### **階段 6: 錯誤處理測試** (預計 5 分鐘)

#### 6.1 API Key 錯誤測試
1. 在 Popup 更換 API Key 為無效值 (例如 `sk-proj-invalid`)
2. 啟用字幕功能
3. 觀察錯誤處理

**驗證點**:
- ✅ Console 顯示 `[ErrorHandler] API Key 無效`
- ✅ 使用者看到錯誤提示 (Popup 或通知)

#### 6.2 網路錯誤測試
1. 開啟影片並啟用字幕
2. 在 DevTools Network tab 中選擇 "Offline" 模式
3. 觀察錯誤處理

**驗證點**:
- ✅ Console 顯示 `[ErrorHandler] 網路錯誤`
- ✅ 先前已顯示的字幕保留，新字幕停止產生

#### 6.3 影片切換測試
1. 啟用字幕後，前往另一個影片頁面
2. 觀察 Extension 是否正確清理資源

**驗證點**:
- ✅ Console 顯示 `[SubtitleService] 已停用`
- ✅ Popup 顯示「未啟用」狀態

---

### **階段 7: 效能測試** (預計 10 分鐘)

#### 7.1 延遲測試
記錄以下時間點並計算總延遲：

1. **音訊發生時間**: 例如影片 10.00 秒處有人說話
2. **字幕顯示時間**: 觀察字幕實際出現的影片時間戳

**計算公式**:
```
總延遲 = 字幕顯示時間 - 音訊發生時間
```

**預期結果**:
```
總延遲 = 5.3 - 6.5 秒
= 3秒累積 + 0.5秒編碼 + 2-3秒 Whisper API
```

**在 Service Worker Console 驗證各階段時間**:
```javascript
// MP3 編碼時間
[MP3 Worker] 編碼完成 { duration: "450.23ms" }  // 應 < 500ms

// Whisper API 時間 (透過時間戳計算)
[SubtitleService] Whisper 辨識完成 // 距離上一行約 2-3 秒
```

#### 7.2 記憶體測試
1. 在 DevTools Performance Monitor 中觀察記憶體使用
2. 播放 5 分鐘影片
3. 檢查是否有記憶體洩漏

**預期結果**:
- ✅ 記憶體使用穩定增長 (segments 累積)
- ✅ 無突然飆升
- ✅ 停用字幕後記憶體釋放

#### 7.3 CPU 使用率
觀察 Task Manager (`chrome://system/`) 中的 CPU 使用率：

**預期結果**:
- ✅ Service Worker: < 10% CPU (非編碼時)
- ✅ MP3 Worker 編碼時: 短暫飆升至 30-50% (< 500ms)
- ✅ 不影響影片播放流暢度

---

## 📊 測試檢查清單

### ✅ 核心功能
- [ ] Extension 成功載入
- [ ] API Key 驗證與加密儲存
- [ ] 音訊擷取權限請求
- [ ] 音訊 Rolling Window 切塊 (3秒 + 1秒重疊)
- [ ] MP3 編碼完成 (< 500ms)
- [ ] Whisper API 辨識成功
- [ ] OverlapProcessor 去重 (15-25% 過濾率)
- [ ] Content Script 字幕顯示
- [ ] VideoMonitor 時間同步

### ✅ 字幕品質
- [ ] 字幕文字正確
- [ ] 斷句自然 (無句子被截斷)
- [ ] 時間戳準確 (與影片同步)
- [ ] 重複字幕已過濾

### ✅ 使用者體驗
- [ ] 字幕樣式美觀 (黑底白字，圓角)
- [ ] 字幕位置正確 (影片下方居中)
- [ ] 淡入動畫流暢
- [ ] 播放/暫停/跳轉正確響應
- [ ] 總延遲 < 7 秒

### ✅ 錯誤處理
- [ ] 無效 API Key 錯誤提示
- [ ] 網路錯誤處理
- [ ] 影片切換資源清理

### ✅ 效能指標
- [ ] MP3 編碼 < 500ms
- [ ] Whisper API 響應 2-3 秒
- [ ] 總延遲 5.3-6.5 秒
- [ ] CPU 使用率正常
- [ ] 無記憶體洩漏

---

## 🐛 常見問題除錯

### 問題 1: 字幕未顯示
**檢查點**:
1. Console 是否有 `[VideoMonitor] 已附加到 video 元素`?
   - ❌ 沒有 → video 元素未找到，檢查頁面是否有 `<video>` 標籤
2. Console 是否有 `[ContentScript] 接收字幕資料`?
   - ❌ 沒有 → Background 未發送字幕，檢查 Whisper API
3. 檢查 CSS 是否載入:
   ```javascript
   document.querySelector('#babel-bridge-subtitle-overlay')
   ```

### 問題 2: API 呼叫失敗
**檢查點**:
1. API Key 是否有效? (在 Popup 重新驗證)
2. OpenAI 帳戶是否有額度?
3. Network tab 是否有 401 或 429 錯誤?

### 問題 3: 延遲過高 (> 8 秒)
**檢查點**:
1. Whisper API 響應時間是否正常? (應 2-3 秒)
2. 網路連線是否穩定?
3. MP3 編碼是否過慢? (應 < 500ms)

### 問題 4: 過濾率異常
**過濾率 > 40%**:
- 檢查 `OVERLAP_CONFIG.similarityThreshold` (預設 0.8)
- 可能設定過低，導致過度過濾

**過濾率 < 5%**:
- Whisper 在重疊區產生不一致結果
- 檢查音訊品質，考慮增加重疊區長度

---

## 📝 測試記錄範本

```markdown
## Phase 1 測試記錄

**測試日期**: 2025-11-09
**測試者**: [你的名字]
**Extension 版本**: [版本號]

### 測試環境
- Chrome 版本: [版本號]
- 作業系統: [macOS/Windows/Linux]
- 測試影片: [影片連結或描述]
- 影片長度: [X 分鐘]
- 影片語言: [英文/中文/...]

### 功能測試結果
- [ ] ✅/❌ Extension 載入
- [ ] ✅/❌ API Key 驗證
- [ ] ✅/❌ 音訊擷取
- [ ] ✅/❌ MP3 編碼
- [ ] ✅/❌ Whisper 辨識
- [ ] ✅/❌ OverlapProcessor
- [ ] ✅/❌ 字幕顯示
- [ ] ✅/❌ 時間同步

### 效能指標
- MP3 編碼平均時間: [X] ms
- Whisper API 平均響應: [X] 秒
- 總延遲: [X] 秒
- OverlapProcessor 平均過濾率: [X]%

### 發現的問題
1. [問題描述]
2. [問題描述]

### 備註
[其他觀察或建議]
```

---

## 🎉 測試完成標準

**Phase 1 視為通過的條件**:
1. ✅ 所有核心功能正常運作
2. ✅ 總延遲 < 7 秒
3. ✅ 字幕與影片完美同步
4. ✅ 無嚴重錯誤或崩潰
5. ✅ 效能指標符合預期

**如果通過** → 🎊 進入 Phase 2 (UI 優化)
**如果未通過** → 📝 記錄問題，回到開發階段修復

---

祝測試順利！🚀
